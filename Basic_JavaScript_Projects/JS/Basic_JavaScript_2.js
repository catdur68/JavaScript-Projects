function myFunction() {
     var sentence = "It looks like each function";
     sentence += " needs to be saved in its own JS file.";
     document.getElementById("concatenate").innerHTML = sentence;
}
